# DeltaForceQJT v1.1.1

《三角洲行动》体验优化辅助工具。仅对个人公开游戏操作做自动化辅助，**不注入、不修改游戏进程内存**。

> 本版本主程序已更名为 **`DeltaForceQJT.exe`**（原 `DeltaForceNormalPlayerHelper.exe`）。升级用户请使用新 exe，旧 exe 可直接删除。

---

## 📦 下载（共 2 个文件，请放在**同一个目录**）

| 文件 | 大小 | 是否必需 | 说明 |
| --- | --- | --- | --- |
| `DeltaForceQJT-v1.1.1.zip` | 876 KB | ✅ 必需 | 主程序 + 界面资源 + 音效 + 简谱，解压即用 |
| `DeltaForceCodeAssistant.exe` | 142 MB | ⚪ 可选 | 自动解锁助手，**仅「自动解锁电脑」需要**；不用该功能可不下载 |

解压后的目录结构（`PicRes\` 必须与主程序同级，否则界面空白）：

```
DeltaForceQJT.exe               # 主程序
PicRes\                         # 界面资源（必需，37 个文件）
assets\                         # 钓鱼音效（3 个 wav）
config\                         # 助手配置
music\                          # 演奏简谱
DeltaForceCodeAssistant.exe     # 可选：放到本级目录即可启用自动解锁
```

---

## ✨ 本次更新（v1.1.1，2026-09-26）

### 🔴 修复：指纹密码无法识别并自动破译

这是本次的重点修复。原实现有三处叠加缺陷，已全部解决：

1. **模板库不全** —— 缺少 3 个角色（埃德温 / 巴斯特 / 雅各布），且卢卡斯缺 5、6 号模板。
   遇到缺失角色时直接「找不到模板」退出。现已补齐为 **9 个角色 / 54 张模板**。
2. **模式判定过于严格** —— 旧逻辑要求 OCR 读出的最大数字**必须正好**是 4 / 6 / 8，
   一旦漏读（例如实际是 8，只认出 3 和 5）整条流程就在第一步中止。
   现改为**区间分档**（>6 判 8，>4 判 6，否则判 4），漏读也能正常跑完。
3. **缺少强制序列与去重** —— 游戏规则是按 1→N 依次点完所有指纹；旧实现既没有按规则
   生成 `[1..N]` 的点击序列，也没有处理「两个候选格抢同一个编号」的情况，导致点错 / 漏点。

同时把模板匹配改为**核心区匹配**（四周各裁 15% 后再比对），显著提升指纹图案轻微滑动时的容错。

### 🔴 修复：配置保存后重启不生效

退出流程里有一句是「关掉所有功能开关，好让功能线程收尾」，而**保存配置写在了这一步之后** ——
于是每次退出都会把「9 个开关全是关」写进 `DFConfig.cfg`，下次启动读到的自然是全部关闭，
表现就是明明保存并应用了配置，重开却失效，得重新打开功能开关。
现已把保存挪到关开关**之前**，并删掉了之后那次重复保存。

### 自动破译交互按键统一

| 按键 | 功能 |
| --- | --- |
| `F5` | 摩斯码识别 → OCR 解码 → 自动输入数字 → 延迟 **0.6 秒** → 点击确认坐标 |
| `F6` | 指纹识别 → 模板匹配 → 自动点击 / 保存标注图 |
| `F7` | 密码门破译（由主程序输入当日密钥） |
| `End` | 退出程序 |
| `p` | 预览配置区域（截图标注后自动打开） |
| `r` | 重新加载配置文件（改完坐标不用重启） |

> **旧组合键已全部移除**：摩斯码 `Ctrl+Alt+Q`、指纹 `Ctrl+Alt+W`、密码门 `Ctrl+Alt+E` 均不再绑定，请改用 `F5` / `F6` / `F7`。

---

## 🔒 SHA-256 校验值

| 文件 | SHA-256 |
| --- | --- |
| `DeltaForceQJT-v1.1.1.zip` | `9a0e6cf82c9ccefe9f37354743eef619bf02e0adde5331873ac2da6b2a58938b` |
| `DeltaForceQJT.exe` | `a4a6c5545fc5eb590a6d5bbc2842895f3f52af36bc3f0ef3a83a9d654041b291` |
| `DeltaForceCodeAssistant.exe` | `f13e6675e4fcc1a9707014815f0f4baea9d7e3c352ca1646c2f75430009dbfdb` |

---

## ⚙️ 使用步骤

1. 下载 `DeltaForceQJT-v1.1.1.zip` 并解压到任意目录（迁移到新电脑请**整包复制**，不要拆分文件）。
2. 需要「自动解锁电脑」时，把 `DeltaForceCodeAssistant.exe` 放到同一目录。
3. 以**管理员身份**运行 `DeltaForceQJT.exe`（关闭 ACE 服务、自动解锁全局热键均需管理员权限）。
4. 在「功能开关」页开启所需功能。
5. 首次使用请在「卡密管理」页完成授权激活，并保持网络畅通以完成授权心跳。

**自动解锁热键**：摩斯码 `F5` ｜ 指纹 `F6` ｜ 密码门 `F7`（旧组合键 `Ctrl+Alt+Q/W/E` 仍可用）

> **确认按钮坐标需先标定**：`morse_confirm_clicks` 默认为空，此时 F5 只做「识别并输入」，不点确认。
> 要启用自动点击确认，请在助手目录执行：
> `DeltaForceCodeAssistant.exe --pickpos`
> 然后把鼠标移到游戏里的「下载 / 确认」按钮上，等待倒数结束即可自动写入配置。

**环境要求**：Windows 10/11 64 位。主程序静态链接运行时，**无需安装 VC++ 运行库**；助手已内置 Python 与 OCR 引擎，**无需安装 Python 或微信**。

---

## ⚠️ 注意事项

- 少数杀毒软件可能对键鼠模拟类工具误报，需自行添加信任。
- 主程序内置完整性自校验，任何对 exe 的篡改都会导致启动自检失败并退出。
- 自动钓鱼为时序型实现，延迟需按钓点 / 手感微调；宏类功能存在被反作弊判定的风险，请自行评估。

---

## 免责声明

本工具仅供个人非商业学习研究使用。使用前请阅读并遵守《三角洲行动》用户协议及相关法律法规；若运营方禁止此类工具请立即停止使用。使用本工具产生的一切后果由使用者自行承担。禁止用于商业代练、倒卖或规模化分发。
